{
  "name": "sourcedeck-server",
  "version": "0.1.0",
  "private": true,
  "description": "SourceDeck API backend — additive brownfield service alongside the static site. Container-portable to IBM Code Engine, OpenShift, and Satellite.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --env-file=.env --watch server.js",
    "dev:nodotenv": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test test/*.test.js && npm run check:privacy",
    "lint": "node --check server.js && find src test -name '*.js' -exec node --check {} +",
    "verify:watsonx": "node --env-file=.env scripts/verify-watsonx.mjs",
    "verify:agents": "node --env-file=.env scripts/verify-watsonx.mjs",
    "check:private-data": "node ../scripts/check-private-data.js",
    "check:demo-parity": "node ../scripts/check-demo-parity.js",
    "check:privacy": "npm run check:private-data && npm run check:demo-parity"
  },
  "dependencies": {
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.22.0"
  }
}
