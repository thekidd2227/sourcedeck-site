apiVersion: v1
kind: ServiceAccount
metadata:
  name: sourcedeck-api
  # Annotate with IBM Cloud IAM trusted profile when running on
  # IKS / ROKS / Satellite to enable workload identity:
  #   eks.amazonaws.com/role-arn: ...   (AWS)
  #   iam.cloud.ibm.com/trusted-profile: <crn>   (IBM)
