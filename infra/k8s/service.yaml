apiVersion: v1
kind: Service
metadata:
  name: sourcedeck-api
  labels:
    app: sourcedeck-api
spec:
  type: ClusterIP
  selector:
    app: sourcedeck-api
  ports:
    - name: http
      port: 80
      targetPort: http
