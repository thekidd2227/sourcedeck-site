# TEMPLATE — do not commit real values. Apply via sealed-secrets, External
# Secrets Operator, or your IBM IAM-backed secrets pipeline.
apiVersion: v1
kind: Secret
metadata:
  name: sourcedeck-secrets
type: Opaque
stringData:
  SESSION_SECRET: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  DATABASE_URL: "postgres://REPLACE"

  IBM_CLOUD_API_KEY: "REPLACE"
  IBM_COS_ENDPOINT: "https://s3.us-east.cloud-object-storage.appdomain.cloud"
  IBM_COS_BUCKET: "REPLACE"
  IBM_COS_INSTANCE_CRN: "REPLACE"
  IBM_COS_API_KEY: "REPLACE"
  IBM_COS_SERVICE_INSTANCE_ID: "REPLACE"

  WATSONX_API_KEY: "REPLACE"
  WATSONX_PROJECT_ID: "REPLACE"

  AUTH_CLIENT_ID: "REPLACE"
  AUTH_CLIENT_SECRET: "REPLACE"
  AUTH_ISSUER_URL: "https://iam.cloud.ibm.com/identity"
